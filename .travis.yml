dist: xenial
services:
  - xvfb
language: python
python:
  - "3.7"

install:
  - pip install -r requirements.txt
  - pip install .

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3

script:
  - xvfb-run make test
  - pytest
