dist: xenial

language: python
python:
  - "3.7"

addons:
  apt:
    packages:
    - libxkbcommon-x11-0

install:
  - pip install -r requirements.txt
  - python setup.py install

script:
  - xvfb-run -a pytest -s -v
